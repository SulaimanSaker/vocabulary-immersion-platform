---
name: stack-preference-frontend
description: User prefers React + TypeScript + Vite for web frontends
metadata:
  type: feedback
---

For web app frontends, the user prefers **React + TypeScript + Vite**.

**Why:** Stated directly when scaffolding the vocabulary-immersion-platform; corrected an initial vanilla-HTML/JS plan.

**How to apply:** Default to a Vite + React + TS client (separate from the backend, with a dev proxy to the API) rather than vanilla HTML/JS or a server-rendered page, unless the user says otherwise.
